<Window x:Class="AIDashboard.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="AI Dashboard" Height="720" Width="1280">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <!-- WebView2 for AI Dashboard Web UI -->
        <wv2:WebView2 Name="webView" Grid.Column="0" />

        <!-- Side Panel for SSH Connection and SFTP Drag & Drop -->
        <Grid Grid.Column="1" Background="#f0f0f0" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- SSH Connection Control -->
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="SSH Connection" FontWeight="Bold" FontSize="16" Margin="0,0,0,10" />
                <TextBox Name="txtHost" Margin="0,0,0,5" Text="127.0.0.1" />
                <TextBox Name="txtUser" Margin="0,0,0,5" Text="username" />
                <PasswordBox Name="txtPassword" Margin="0,0,0,10" PasswordChar="*" />
                <Button Content="Connect &amp; Tunnel" Click="btnConnect_Click" Padding="5" />
                <TextBlock Name="txtStatus" Margin="0,10,0,0" Foreground="Blue" />
            </StackPanel>

            <!-- SFTP Dropzone -->
            <Border Grid.Row="2" Background="LightGray" BorderBrush="Gray" BorderThickness="2" CornerRadius="5"
                    AllowDrop="True" Drop="Dropzone_Drop">
                <TextBlock Text="Drag &amp; Drop Files Here&#x0a;to Upload via SFTP" 
                           HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"
                           Foreground="DarkGray" FontSize="14" />
            </Border>

            <!-- SFTP Progress -->
            <StackPanel Grid.Row="3" Margin="0,10,0,0">
                <TextBlock Name="txtUploadStatus" Text="Ready" Margin="0,0,0,5" />
                <ProgressBar Name="uploadProgress" Height="20" Minimum="0" Maximum="100" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
